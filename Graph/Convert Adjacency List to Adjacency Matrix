class Solution {
    public int[][] adjToMat(ArrayList<ArrayList<Integer>> adj) {
        int n=adj.size();
        int[][] mat=new int[n][n];
        for(int i=0;i<n;i++){
            for(int ele:adj.get(i)){
                mat[i][ele]=1;
            }
        }
        return mat;
    }
}
